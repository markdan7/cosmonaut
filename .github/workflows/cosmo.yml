name: cosmonaut-macos
on: workflow_dispatch
jobs:
  run-cosmo:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show macOS fragment
        run: |
          chmod +x ./cosmonaut.com
          ./cosmonaut.com | sed -n '/Like on macOS!/{n;p;q}'
